# Backend API Base URL (Leave empty or set to your deployed backend URL on Render, Railway, or VPS)
# When empty in local development, requests are proxied via Vite to http://127.0.0.1:8000/api
VITE_API_BASE_URL=
